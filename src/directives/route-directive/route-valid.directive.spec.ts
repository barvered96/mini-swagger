import { RouteValidDirective } from './route-valid.directive';

describe('RouteValidDirective', () => {
  it('should create an instance', () => {
    const directive = new RouteValidDirective();
    expect(directive).toBeTruthy();
  });
});
