import { UrlValidDirective } from './url-valid.directive';

describe('UrlValidDirective', () => {
  it('should create an instance', () => {
    const directive = new UrlValidDirective();
    expect(directive).toBeTruthy();
  });
});
